<!DOCTYPE html>
<html>
	<head>
  		<title>DictyConf</title>
  		<%= stylesheet_link_tag    "application", "bootstrap.min", :media => "all" %>
  		<%= javascript_include_tag "application", "jquery.min", "bootstrap.min" %>
  		<%= csrf_meta_tags %>
	</head>

	<body>
		<div class="container">
			
			<%= render :file => 'layouts/nav-bar' %>
			
			<div class="container">
				<%= content_for?(:content) ? yield(:content) : yield %>
			</div>

			<%= render :file => 'layouts/footer' %>

		</div>
	</body>
</html>
